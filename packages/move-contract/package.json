{
  "name": "@devote/move-contract",
  "version": "1.0.0",
  "private": true,
  "description": "Aptos Move smart contracts for DeVote.",
  "scripts": {
    "compile": "aptos move compile --named-addresses devote=0xCAFE",
    "test": "aptos move test",
    "fmt": "aptos move fmt",
    "publish:contract": "aptos move publish --named-addresses devote=default --assume-yes"
  }
}
